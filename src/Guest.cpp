#include "Activity.h"
#include "Guest.h"

// Registers the guest for a specific activity
void Guest::registerForActivity(Activity activity) {

}

// Checks if the guest has a scheduling conflict with the activity
bool Guest::checkConflicts(Activity activity) {

}